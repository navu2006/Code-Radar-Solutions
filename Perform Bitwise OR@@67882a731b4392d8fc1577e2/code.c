#include <stdio.h>

int main() {
    int num1, num2, result;

    // Input two integers from the user
    printf("%d", num1, num2);
    scanf("%d %d", &num1, &num2);

    // Perform bitwise OR operation
    result = num1 | num2;

    // Output the result
    printf("%d ", %d|%d);

    return 0;
}
